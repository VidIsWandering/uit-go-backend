# Driver Service environment variables
# Run standalone (outside docker-compose)
# For docker-compose, REDIS_URL is set by compose automatically

REDIS_URL=redis://localhost:6379
# Optional: override server port (default 8082)
# PORT=8082
