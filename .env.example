# file: .env.example
# Đây là file mẫu. Hãy copy file này thành .env và điền các giá trị của bạn.

# ===== CSDL cho UserService (Java) =====
USER_DB_USER=admin
USER_DB_PASSWORD=secret_password_user
USER_DB_NAME=uit_user_db

# ===== CSDL cho TripService (Java) =====
TRIP_DB_USER=admin
TRIP_DB_PASSWORD=secret_password_trip
TRIP_DB_NAME=uit_trip_db

# ===== Shared Secrets =====
JWT_SECRET=my_super_secret_jwt_key_123456_longer_now_32_chars

# ===== CSDL cho DriverService (Node.js) =====
# (Redis config defaults are usually fine, but can be overridden here)
